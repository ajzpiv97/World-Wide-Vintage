import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import Root from './Root';

export default () => {
  return (
    <NavigationContainer>
      <Root/>
    </NavigationContainer>
  );
}
